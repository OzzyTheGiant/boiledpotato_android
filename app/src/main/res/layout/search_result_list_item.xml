<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="@dimen/padding_viewport"
    android:layout_marginBottom="@dimen/padding_viewport"
    card_view:cardCornerRadius="@dimen/border_radius"
    card_view:cardBackgroundColor="@color/accent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/recipe_image"
            android:layout_width="match_parent"
            android:layout_height="@dimen/search_result_image_view_height"
            android:scaleType="centerCrop"
            android:background="@color/cardview_light_background"
            tools:ignore="ContentDescription"/>

        <TextView
            android:id="@+id/recipe_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="@dimen/padding_viewport"
            android:text="@string/placeholder_text"
            android:textColor="#FFFFFF"
            android:textSize="@dimen/font_size_heading_search_result"
            android:lines="2"
            android:maxLines="2"/>

    </LinearLayout>

</androidx.cardview.widget.CardView>